# Optional media origin override. Production defaults to the R2 custom domain
# below; local development falls back to the checked-in images and public/
# files when this variable is unset.
VITE_MEDIA_BASE_URL=https://media.avinashpatel.in
